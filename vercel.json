{
  "framework": "nextjs",
  "buildCommand": "export DATABASE_URL_UNPOOLED=\"${DATABASE_URL_UNPOOLED:-$DATABASE_URL}\" && prisma generate && prisma migrate deploy && tsx prisma/seed.ts && next build",
  "installCommand": "npm install"
}
